{
  "name": "car-tuning-telegram-mvp",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm --workspace @mvp/shared run build && npm --workspace @mvp/db run build && npm --workspace @mvp/image run build && npm --workspace @mvp/api run build && npm --workspace @mvp/worker run build && npm --workspace @mvp/bot run build",
    "dev": "npm run -ws dev",
    "test": "npm --workspace @mvp/shared run test",
    "lint": "npm run -ws lint",
    "format": "prettier --write .",
    "migrate": "npm --workspace @mvp/db run migrate"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.16.0",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}